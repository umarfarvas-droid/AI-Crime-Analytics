spring:
  datasource:
    url: jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE;MODE=PostgreSQL
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: create-drop
    show-sql: false

app:
  jwt:
    secret: test-secret-key-that-is-at-least-256-bits-long-for-hmac-sha-algorithm-validation-in-unit-tests
    expiration: 3600000
  ai:
    openai-api-key: ""
    model: gpt-4
    temperature: 0.7
